server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:authentication-dev
    driverClassName: org.h2.Driver
    username: admin
    password: password
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false

management:
  endpoint:
    health:
      enabled: true
    info:
      enabled: true
  endpoints:
    web:
      discovery:
        enabled: false
    enabled-by-default: true

logging:
  config: src/main/resources/logback-dev.xml
