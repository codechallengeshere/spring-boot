server:
  port: 9090

spring:
  datasource:
    url: jdbc:h2:mem:authentication-test
    driverClassName: org.h2.Driver
    username: admin
    password: password
  jpa:
    open-in-view: false

management:
  endpoint:
    health:
      enabled: true
    info:
      enabled: true
  endpoints:
    web:
      discovery:
        enabled: false
    enabled-by-default: true

logging:
  config: src/test/resources/logback-test.xml